[package]
name = "score"
version = "0.1.0"
edition = "2021"

[dependencies]
graphics = { path = "../graphics" }
core = { path = "../core" }
core_parse = { path = "../core_parse" }
utils = { path = "../utils" }
grid = { path = "../grid" }
