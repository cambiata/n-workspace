[package]
name = "score"
version = "0.1.0"
edition = "2024"

[dependencies]
graphics = { path = "../graphics" }
core = { path = "../core" }
parse = { path = "../parse" }
utils = { path = "../utils" }
grid = { path = "../grid" }
